# Server Environment Configuration

# Port for the server to listen on
PORT=4242
# Host for the server to bind to
HOST=0.0.0.0

# Database Configuration
# Database Dialect: sqlite or postgresql
# SQLite is for desktop mode (default)
# PostgreSQL is for web mode
DATABASE_DIALECT=sqlite

# Database URL
# For SQLite: relative or absolute path to .db file
DATABASE_URL=./data/kontexted.db

# For PostgreSQL: connection string (only used when DATABASE_DIALECT=postgresql)
# DATABASE_URL=postgresql://user:password@host:5432/database

# Authentication Configuration
# Auth Method: email-password or keycloak
# If not set, will auto-detect based on Keycloak credentials
# AUTH_METHOD=email-password

# Keycloak Configuration (required if using keycloak method)
# AUTH_KEYCLOAK_ID=kontexted-webapp
# AUTH_KEYCLOAK_SECRET=kontexted-local-secret
# AUTH_KEYCLOAK_ISSUER=http://localhost:8080/realms/kontexted

# Better Auth Configuration
BETTER_AUTH_SECRET=change-me-in-production
BETTER_AUTH_URL=http://localhost:4242
BETTER_AUTH_TRUSTED_ORIGINS=http://localhost:3000,http://localhost:5173

# Invite Code for Sign Up (required for email/password authentication)
# If empty, sign-up will be disabled
INVITE_CODE=my-secret-code

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:4242

# Collab Service (WebSocket collaboration server)
COLLAB_TOKEN_SECRET=dev-secret

# Node Environment
NODE_ENV=development

# Logging
LOG_LEVEL=info
